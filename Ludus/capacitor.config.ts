import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ludus.chatarum.es',
  appName: 'LudusChatarum',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
